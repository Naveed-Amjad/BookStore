<p style="color: green"><%= notice %></p>

<h1>Comments</h1>

<div id="comments">
  <% @comments.each do |comment| %>
    <%= render comment %>
    <p>
      <%= link_to "Show this comment", book_comment_path(id: comment.id) %>
    </p>
  <% end %>
</div>

<%= link_to "New comment", book_comments_path %>
