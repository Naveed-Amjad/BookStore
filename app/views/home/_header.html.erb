<nav class="navbar navbar-expand-lg text-white bg-black">
  <div class="container-fluid">
    <%= link_to 'BookStore', root_path, class:"nav-link text-white fw-bold fs-4" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <li class="nav-item">
          <%= link_to 'About Us', home_about_path, class:"nav-link text-white fw-bold" %>
        </li>

        <li class="nav-item">
          <%= link_to 'Books', books_path, class:"nav-link text-white" %>
        </li>

        <% if user_signed_in? %>
          <li class="nav-item">
            <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class:"nav-link text-white btn btn-danger" %>
          </li>

          <li class="nav-item">
            <%= link_to 'Edit Profile', edit_user_registration_path, class:"nav-link text-white btn btn-outline-secondary" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign In', new_user_session_path, class:"nav-link text-white btn btn-outline-light" %>
          </li>

          <li class="nav-item">
            <%= link_to 'Sign Up', new_user_registration_path, class:"nav-link text-white btn btn-outline-light" %>
          </li>
        <% end %>

      </ul>

      <%= link_to "Add New book", new_book_path, class:"btn btn-success me-3" %>

      <% if current_user.present? %>
        <button class="btn btn-outline-success"><%= current_user&.role  %></button>
      <% end %> 
    </div>
  </div>
</nav>
